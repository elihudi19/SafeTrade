# --- Core framework ---
Django==5.0.6
djangorestframework==3.15.2
django-environ==0.11.2

# --- Database ---
psycopg2-binary==2.9.9

# --- Background tasks / offline sync ---
celery==5.4.0
redis==5.0.4

# --- Storage abstraction (works with Render disk, AWS S3, or any S3-compatible service) ---
django-storages==1.14.3
boto3==1.34.106

# --- Auth ---
djangorestframework-simplejwt==5.3.1

# --- Prod server ---
gunicorn==22.0.0
whitenoise==6.6.0

# --- Bolt / external HTTP calls ---
requests==2.32.3

# --- Scheduled Celery tasks (predictive analytics, SMS resend) ---
django-celery-beat==2.6.0
